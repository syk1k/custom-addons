<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="moodle.list">
      <field name="name">moodle list</field>
      <field name="model">moodle.moodle</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="moodle.action_window">
      <field name="name">moodle window</field>
      <field name="res_model">moodle.moodle</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="moodle.action_server">
      <field name="name">moodle server</field>
      <field name="model_id" ref="model_moodle_moodle"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="moodle" id="moodle.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="moodle.menu_1" parent="moodle.menu_root"/>
    <menuitem name="Menu 2" id="moodle.menu_2" parent="moodle.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="moodle.menu_1_list" parent="moodle.menu_1"
              action="moodle.action_window"/>
    <menuitem name="Server to list" id="moodle" parent="moodle.menu_2"
              action="moodle.action_server"/>
    -->
  </data>
</odoo>